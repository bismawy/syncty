{
  "name": "syncty",
  "version": "1.1.3",
  "private": true,
  "description": "Syncty — bookmark sync extension (zero-knowledge E2E) for Chromium & Firefox.",
  "workspaces": [
    "extension",
    "backend"
  ],
  "scripts": {
    "dev:ext": "bun run --filter syncty-extension dev",
    "dev:api": "bun run --filter syncty-backend dev",
    "dev": "concurrently -n ext,api -c blue,green \"bun run dev:ext\" \"bun run dev:api\"",
    "build:ext": "bun run --filter syncty-extension build",
    "build:api": "bun run --filter syncty-backend build",
    "zip:ext": "bun run --filter syncty-extension zip",
    "deploy:api": "bun run --filter syncty-backend deploy",
    "typecheck": "bun run --filter syncty-extension typecheck && bun run --filter syncty-backend typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}